services:
  celery_worker:
    volumes:
    - ./:/app:z
    - ./persist/static:/app_static
    - ./persist/media:/app_media
  celery_beat:
    volumes:
    - ./:/app:z
    - ./persist/static:/app_static
    - ./persist/media:/app_media
  celery_flower:
    volumes:
    - ./:/app:z
    - ./persist/static:/app_static
    - ./persist/media:/app_media
  server:
    volumes:
    - ./:/app:z
    - ./persist/static:/app_static
    - ./persist/media:/app_media
